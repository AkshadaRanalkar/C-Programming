/*
•	NULL pointer:
Null is internally considered as a Macro which is defined in stdio.h header file as #define NULL (void*) 0

According to this syntax NULL is considered as 0 means the pointer is pointing 0th address of RAM.

*/

/*
• Array and Pointers:
We can create the pointer which points to the elements of array.
*/
