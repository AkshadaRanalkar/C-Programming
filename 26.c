/*
•	Void Pointer:
Void pointer is called as generic pointer. Void pointer is a pointer which can store address of any data type.

- In the above example, the void pointer vp initially points to the character and then it will point to the integer.
- If we want to fetch the data using void pointer then we must use the concept of type casting.
- Type casting is the concept where the pointer gets converted into the other data type for the temporary period.

*/